## Packages
framer-motion | Smooth page transitions, list animations, and micro-interactions
date-fns | Human-readable date formatting (e.g., relative times)
lucide-react | Beautiful, consistent icons across the UI

## Notes
- Fake auth is implemented via localStorage for demo purposes
- Search uses a POST request per API contract, implemented inside a useQuery for caching
- The UI language is primarily Russian, adhering to the design specifications
- Relies on standard Tailwind utilities and some custom CSS variables for styling
